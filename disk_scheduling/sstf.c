#include<stdio.h>
#include<math.h>

int main(){
	//this algorithm seeks the closest possible task to the present location of the header
	int queue[100].t[100],seek=0,n,i,j,temp;
	float avg;
	printf("Enter size of queue");
	scanf("%d",&n);
	printf("Enter queue");
	for(i=0;i<n;i++){scanf("%d",&queue[i]);}

}
